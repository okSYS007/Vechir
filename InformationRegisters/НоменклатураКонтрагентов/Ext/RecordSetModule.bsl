
Процедура ПередЗаписью(Отказ, Замещение)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Для каждого Запись Из ЭтотОбъект Цикл
		Если Запись.Номенклатура.Пустая() Тогда
			Отказ = Истина;
			Сообщить(НСтр("ru='Нельзя записывать с незаполненным значением номенклатуры.';uk='Не можна записувати з незаповненим значенням номенклатури.'"));
			Продолжить;
		КонецЕсли; 
		Если Запись.Номенклатура.ЭтоГруппа Тогда
			Отказ = Истина;
			Сообщить(НСтр("ru='Нельзя выбирать группу номенклатуры в качестве номенклатуры контрагента.';uk='Не можна вибирати групу номенклатури в якості номенклатури контрагента.'"));
			Продолжить;
		КонецЕсли; 
	КонецЦикла; 
	
КонецПроцедуры

