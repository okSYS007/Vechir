
Процедура ПередЗаписью(Отказ, Замещение)

	//Если Количество() > 0 Тогда
	//	ТаблицаНабора = Выгрузить();
	//	МассивСтрок = ТаблицаНабора.НайтиСтроки(Новый Структура("ИдентификаторУстройства", ""));
	//	Если МассивСтрок.Количество() > 0 Тогда
	//		Для Каждого СтрокаМассива Из МассивСтрок Цикл
	//			СтрокаМассива.ИдентификаторУстройства = Строка(Новый УникальныйИдентификатор);
	//		КонецЦикла;
	//		Загрузить(ТаблицаНабора);
	//	КонецЕсли;
	//КонецЕсли;

КонецПроцедуры
