Перем ИзменилсяКод;

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка = Истина Тогда
		Возврат;
	КонецЕсли;
	
	// необходимо перезаписать (пререзаполнить наименования и реквизиты) в справочнике НоменклатураГТД, если изменился код справочника
	Если ИзменилсяКод = Истина Тогда
	
		Запрос = Новый Запрос();
		Запрос.УстановитьПараметр("Ссылка", Ссылка);
		Запрос.Текст = "ВЫБРАТЬ
		               |	НоменклатураГТД.Ссылка
		               |ИЗ
		               |	Справочник.НоменклатураГТД КАК НоменклатураГТД
		               |ГДЕ
		               |	НоменклатураГТД.КодУКТВЭД = &Ссылка";
					   
		СообщениеОбОшибкеВыдавалось = Ложь;					   
		
		Выборка = Запрос.Выполнить().Выбрать();				   
		Пока Выборка.Следующий() Цикл
		
			Спр = Выборка.Ссылка.ПолучитьОбъект();
			Спр.ОбновлениеИндексныхПолей();
			Спр.ОбменДанными.Загрузка = Истина;
			Попытка
			    Спр.Записать();
			Исключение
				
				Если СообщениеОбОшибкеВыдавалось = Ложь Тогда
					Сообщить(НСтр("ru='Не удалось обновить данные справочника Номенклатура ГТД. Отображение Кода УКТВЭД в списке и в наименовании этого справочника может быть не правильным!';uk='Не вдалося оновити дані довідника Номенклатура ВМД. Відображення Коду УКТЗЕД у списку і в найменуванні цього довідника може бути не правильним'"), СтатусСообщения.Внимание);			
					СообщениеОбОшибкеВыдавалось = Истина;
				КонецЕсли;
				
			КонецПопытки;
		
		КонецЦикла;
	
	КонецЕсли;
	
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
		
	Если ОбменДанными.Загрузка = Истина Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Вид) Тогда
		Сообщить(НСтр("ru='Не заполненно поле ""Вид"" !';uk='Не заповнено поле ""Вид"" !'"));	
		Отказ = Истина;
	КонецЕсли; 
	
	ИзменилсяКод = Ссылка.Код <> Код;
	
КонецПроцедуры
