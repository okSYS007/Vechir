
Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ВидОбъектаКонтактнойИнформации) ИЛИ НЕ ЗначениеЗаполнено(Тип) Тогда
		#Если Клиент Тогда
			Предупреждение(НСтр("ru='Укажите вид и тип объекта контактной информации.';uk=""Вкажіть вид і тип об'єкта контактної інформації."""));
		#КонецЕсли
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры
