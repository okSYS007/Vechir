
Перем ЗапрещенныеСчета Экспорт;
Перем СписокИсключений Экспорт;

Процедура ПередЗаписью(Отказ)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если Забалансовый = 0 Тогда
		Забалансовый = 1;
	КонецЕсли;

КонецПроцедуры

ЗапрещенныеСчета = Новый СписокЗначений();
ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.Запасы);
ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.Оборудование);
ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ТоварыПереданные);

ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ТорговаяНаценкаНТТ);
ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ТоварыВРозничнойТорговлеВПродажныхЦенахНТТ);

СписокИсключений = Новый СписокЗначений();
