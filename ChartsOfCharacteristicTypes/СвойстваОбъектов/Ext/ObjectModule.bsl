Перем КонтролироватьПорядок Экспорт;

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ

// Обработчик события ПередЗаписью объекта.
//
Процедура ПередЗаписью(Отказ)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоНовый() 
	   И НазначениеСвойства <> Ссылка.НазначениеСвойства 
	   И ПолныеПрава.СвойстваОбъектов_СуществуютСсылки(Ссылка) Тогда

		Сообщить(НСтр("ru='Существуют объекты, которым назначено свойство ""';uk='Існують об''єкти, яким призначена властивість ""'") + Наименование + НСтр("ru='""."
"Назначение свойства не может быть изменено, элемент не записан.';uk='""."
"Призначення властивості не може бути змінене, елемент не записаний.'"), 
		         СтатусСообщения.Важное);

		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры

// Обработчик события ПередУдалением объекта.
//
Процедура ПередУдалением(Отказ)

	Если Предопределенный Тогда
		Сообщить(НСтр("ru='Не допускается удаление предопределенных элементов!';uk='Не допускається вилучення напередвизначених елементів!'"),СтатусСообщения.Внимание);
		Отказ = Истина;
	КонецЕсли;

КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ЭКСПОРТИРУЕМЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

КонтролироватьПорядок = Истина;
